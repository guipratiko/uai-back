# EasyPanel — API + geração de PNG do ingresso (sharp/librsvg precisa de fontes)
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "fontconfig", "dejavu_fonts"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]
